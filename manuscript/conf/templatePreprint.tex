%!TEX TS-program = xelatex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Preprint template — single column, readable, neat
% Will Vieira
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[11pt]{article}

%--- page geometry -----------------------------------------------------------
\usepackage{geometry}
\geometry{letterpaper,
          tmargin=2.4cm, bmargin=2.4cm,
          lmargin=2.6cm, rmargin=2.6cm,
          headsep=14pt, footskip=28pt}

%--- spacing & paragraphs ----------------------------------------------------
\usepackage{setspace}
\setstretch{1.15}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.5em plus 0.1em minus 0.1em}

%--- fonts -------------------------------------------------------------------
\usepackage{ifxetex,ifluatex}
\ifnum 0\ifxetex 1\fi\ifluatex 1\fi=0
  % pdflatex
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \IfFileExists{libertinus.sty}{\usepackage{libertinus}}{%
    \IfFileExists{XCharter.sty}{\usepackage{XCharter}\usepackage[scaled=0.92]{helvet}}{}}
\else
  % xelatex / lualatex — libertinus package auto-loads fontspec + sets faces
  \IfFileExists{libertinus.sty}{\usepackage{libertinus}}{%
    \usepackage{fontspec}
    \defaultfontfeatures{Ligatures=TeX, Scale=MatchLowercase}
    \IfFileExists{XCharter.sty}{\usepackage{XCharter}}{}}
\fi
\usepackage{microtype}

%--- math --------------------------------------------------------------------
\usepackage{amsmath, amssymb}

%--- color -------------------------------------------------------------------
\usepackage{xcolor}
\definecolor{accent}{HTML}{1F5673}     % muted teal-blue for links/rules
\definecolor{soft}{HTML}{6B6B6B}       % soft grey for metadata
\definecolor{rule}{HTML}{C9C9C9}       % light hairline rule

%--- graphics & captions -----------------------------------------------------
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
\makeatletter
\def\fps@figure{htbp}
\makeatother

\usepackage[font=small, labelfont={bf,color=accent}, labelsep=period,
            justification=justified, singlelinecheck=false]{caption}

%--- tables ------------------------------------------------------------------
$if(tables)$
\usepackage{longtable, booktabs}
$endif$

%--- section headings --------------------------------------------------------
\usepackage{titlesec}
\titleformat{\section}
  {\sffamily\large\bfseries\color{accent}}{\thesection}{0.6em}{}
\titleformat{\subsection}
  {\sffamily\normalsize\bfseries}{\thesubsection}{0.5em}{}
\titleformat{\subsubsection}
  {\sffamily\normalsize\itshape}{\thesubsubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{1.4ex plus .4ex minus .2ex}{0.6ex}
\titlespacing*{\subsection}{0pt}{1.0ex plus .3ex minus .2ex}{0.4ex}
\titlespacing*{\subsubsection}{0pt}{0.8ex plus .2ex minus .2ex}{0.3ex}

%--- author block ------------------------------------------------------------
\usepackage{authblk}
\renewcommand\Authfont{\normalsize}
\renewcommand\Affilfont{\footnotesize\itshape\color{soft}}
\setlength{\affilsep}{0.4em}

%--- header / footer ---------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
$if(running_title)$
\fancyhead[L]{\footnotesize\itshape\color{soft}$running_title$}
$else$
$if(title-meta)$
\fancyhead[L]{\footnotesize\itshape\color{soft}$title-meta$}
$endif$
$endif$
\fancyhead[R]{\footnotesize\color{soft}%
$if(authors)$$for(authors)$$authors.familyname$$sep$, $endfor$$endif$}
\fancyfoot[C]{\footnotesize\color{soft}\thepage}

\fancypagestyle{firstpage}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
  \fancyfoot[C]{\footnotesize\color{soft}\thepage}
}

%--- abstract environment (boxed-left rule) ----------------------------------
\usepackage{changepage}
\newenvironment{preprintabstract}
  {\par\medskip
   \begingroup
   \leftskip=0.6em \rightskip=0.6em
   \small
   \noindent\textcolor{accent}{\rule{2.5em}{1.2pt}}\par\smallskip
   \noindent\textsf{\bfseries\color{accent}Abstract}\par\smallskip}
  {\par\endgroup\medskip}

%--- hyperref ----------------------------------------------------------------
\IfFileExists{xurl.sty}{\usepackage{xurl}}{}
\IfFileExists{bookmark.sty}{\usepackage{bookmark}}{\usepackage{hyperref}}
\hypersetup{
$if(title-meta)$
  pdftitle={$title-meta$},
$endif$
$if(authors)$
  pdfauthor={$for(authors)$$authors.givennames$~$authors.familyname$$sep$ and $endfor$},
$endif$
$if(language)$
  pdflang={$language$},
$endif$
$if(keywords)$
  pdfkeywords={$for(keywords)$$keywords$$sep$, $endfor$},
$endif$
  colorlinks=true,
  allcolors=accent,
  pdfcreator={LaTeX via pandoc}}
\urlstyle{same}

%--- pandoc plumbing ---------------------------------------------------------
$if(highlighting-macros)$
$highlighting-macros$
$endif$

\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

$if(numbersections)$
\setcounter{secnumdepth}{$if(secnumdepth)$$secnumdepth$$else$5$endif$}
$else$
\setcounter{secnumdepth}{-\maxdimen}
$endif$

% csl reference list (pandoc)
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newenvironment{cslreferences}%
  {$if(csl-hanging-indent)$\setlength{\parindent}{0pt}%
  \everypar{\setlength{\hangindent}{\cslhangindent}}\ignorespaces$endif$}%
  {\par}
$endif$

\setlength{\emergencystretch}{3em}

$for(header-includes)$
$header-includes$
$endfor$

%--- title block -------------------------------------------------------------
\title{\vspace{-2.0em}%
  {\sffamily\bfseries\fontsize{18}{22}\selectfont\color{accent}$title$}\vspace{0.2em}}

$for(authors)$
\author[$for(authors.affiliations)$$authors.affiliations$$sep$,$endfor$]{%
  $authors.givennames$~$authors.familyname$%
  $if(authors.corresponding)$\thanks{Corresponding author: \href{mailto:$authors.email$}{$authors.email$}}$endif$%
}
$endfor$

$for(affiliations)$
\affil[$affiliations.index$]{$affiliations.name$}
$endfor$

\date{\footnotesize\color{soft}$date$}

%=============================================================================
\begin{document}

% subtle preprint banner
\noindent{\sffamily\footnotesize\color{soft}%
  PREPRINT \textcolor{rule}{\textbar}\ Not peer-reviewed%
  $if(date)$ \textcolor{rule}{\textbar}\ $date$$endif$%
}\par
\vspace{-0.4em}
\noindent\textcolor{rule}{\rule{\linewidth}{0.4pt}}

\maketitle
\thispagestyle{firstpage}
\vspace{-1.2em}

$if(abstract)$
\begin{preprintabstract}
$abstract$
\end{preprintabstract}
$endif$

$if(keywords)$
\noindent{\small\textsf{\bfseries\color{accent}Keywords}\ \ %
$for(keywords)$$keywords$$sep$ \textcolor{rule}{\textbullet}\ $endfor$}
\par\medskip
\noindent\textcolor{rule}{\rule{\linewidth}{0.4pt}}
$endif$

$for(include-before)$
$include-before$

$endfor$

%--- body --------------------------------------------------------------------
$body$

%--- references --------------------------------------------------------------
\newpage

$if(natbib)$
$if(bibliography)$
$if(biblio-title)$
\renewcommand\refname{$biblio-title$}
$endif$
\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}

$endif$
$endif$
$if(biblatex)$
\printbibliography$if(biblio-title)$[title=$biblio-title$]$endif$
$endif$

%--- article information (admin metadata, small, at end) ---------------------
$if(acknowledgments)$$if(funding)$$if(authorship)$$if(conflicts-of-interest)$$if(open_research_statement)$
\bigskip
\noindent\textcolor{rule}{\rule{\linewidth}{0.4pt}}\par
\section*{\sffamily\normalsize\color{accent}Article information}
\begingroup\small\color{soft}
$if(open_research_statement)$\noindent\textbf{Open research statement.} $open_research_statement$\par\smallskip$endif$
$if(acknowledgments)$\noindent\textbf{Acknowledgments.} $acknowledgments$\par\smallskip$endif$
$if(funding)$\noindent\textbf{Funding.} $funding$\par\smallskip$endif$
$if(authorship)$\noindent\textbf{Authorship.} $authorship$\par\smallskip$endif$
$if(conflicts-of-interest)$\noindent\textbf{Conflicts of interest.} $conflicts-of-interest$\par\smallskip$endif$
$if(supporting-files)$\noindent\textbf{Supporting information.} Available $for(supporting-files)$\href{https://$github-user$.github.io/$github-repo$/$supporting-files$.pdf}{here}$sep$, $endfor$.\par$endif$
\endgroup
$endif$$endif$$endif$$endif$$endif$

$for(include-after)$
$include-after$

$endfor$

\end{document}
